node_modules/

# Capacitor web bundle (generated by `npm run build:www` / `npx cap`).
# The ios/ native project IS committed so it can be opened in Xcode directly;
# ios/.gitignore covers its build products.
www/
android/

# Scratch/diagnostic probes (temporary, not part of the suite)
tests/_*.js

# Local, machine-specific Claude Code settings
.claude/settings.local.json
