{
  "name": "xc-dynasty",
  "version": "11.0.0",
  "description": "Cross Country Dynasty — a browser-based college cross country coaching dynasty game.",
  "private": true,
  "scripts": {
    "build:www": "node scripts/build-www.js",
    "ios:add": "npm run build:www && npx cap add ios",
    "ios:sync": "npm run build:www && npx cap sync ios",
    "ios:open": "npx cap open ios",
    "test": "node tests/test-update13-features.js"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "playwright": "^1.61.1"
  }
}
